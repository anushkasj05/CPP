// pre defined string function
// 1.strlen(variable)-it returns the length of the given strings
// 
#include<iostream>
using namespace std;
#include<string.h> // it returns the length of the string
int main()
{
    char a[20];
    char d[5]="UP";
    cout<<strrev(d);
    /*
    cout<<"Enter a string: ";
    cin>>a;
    cout<<strlen(a); //length of the string
    cout<<strupr(a); // upper case
    cout<<strlwr(d); //lower cases
    cout<<strcat(a,d); // string concatenation
    strcpy(a,d); //string copy
    cout<<"s="<<"\n";
    cout<<"enter pwd\n";
    cin>>a;
    if(stricmp(s,"sunday")==0) //string compare
    {
        cout<<"matched";
    }
    else{
        cout<<"not matched";
    }
    */
} 